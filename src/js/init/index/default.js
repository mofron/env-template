/**
 * @file default.js
 * @author simpart
 */
let mf = require('mofron');

module.exports = (rc) => {
    try {
        
    } catch (e) {
        console.error(e.stack);
        throw e;
    }
}
/* end of file */

