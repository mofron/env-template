/**
 * @file theme.js
 * @brief app theme
 */
const mf   = require('mofron');

module.exports = {
    theme : (thm) => {
        try {
            // please define app theme here
        } catch (e) {
            console.error(e.stack);
            throw e;
        }
    }
};
/* end of file */
